<tr>
    <td class="header">
        <a href="{{ $url }}">
            {{ $slot }}
            <img style="height: 32px;" height="32" src="{{ asset('images/logo.svg') }}" alt="{{ config('app.name') }}">
        </a>
    </td>
</tr>
